# CookieCloud 环境变量配置示例
# 复制此文件为 .env 并根据需要修改配置值

# ============================================
# 服务配置
# ============================================

# HTTP 服务端口（默认：8088）
PORT=8088

# API 路径前缀（默认：空，即根路径 /）
# 示例：/api、/v1、/cookiecloud
# 注意：会自动移除尾部的斜杠
API_ROOT=

# 数据存储目录（默认：./data）
# 绝对路径或相对路径均可
DATA_DIR=./data

# ============================================
# 可选配置
# ============================================

# 时区设置（Docker 容器内默认：Asia/Shanghai）
TZ=Asia/Shanghai

# ============================================
# 配置说明
# ============================================
#
# 1. PORT: HTTP 监听端口
#    - 范围：1024-65535（避免使用系统保留端口）
#    - 确保防火墙允许该端口的入站连接
#
# 2. API_ROOT: API 路径前缀
#    - 空字符串表示直接在根路径提供服务
#    - 设置后所有 API 端点都会加上该前缀
#    - 示例：
#      - API_ROOT=/api  → 端点为 /api/update、/api/get/:uuid
#      - API_ROOT=      → 端点为 /update、/get/:uuid
#
# 3. DATA_DIR: 数据持久化目录
#    - 必须确保应用有读写权限
#    - Docker 部署时建议使用卷挂载
#    - 示例：docker run -v $(pwd)/data:/data ...
#
# ============================================
